# Todo

Ordered. Top is next.

1. Draw the Cairn mark properly. `assets/c.txt` is the home-screen logo and the source `scripts/icon.py` renders the app icon from; `assets/s.txt` is the scadoshi mark kept behind the Mark setting. The pipeline is verified, only the artwork is pending.
2. TestFlight and App Store review. `operations/ios/submission.md`.
3. Edit a past day's count (today's +/- exists; history is read-only).
4. Export: pick a destination instead of always writing to Downloads.
5. A yearly heat strip per counter.
