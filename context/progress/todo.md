# Todo

Ordered. Top is next.

1. Use it for a week; note what the counter screen is missing.
2. Edit a past day's count (today's +/- exists; history is read-only).
3. Rename a counter and change its goal after creation.
4. Export: pick a destination instead of always writing to Downloads.
5. iOS build via `dx serve --platform ios`; check the fonts and safe areas.
6. A yearly bar or heat strip per counter (use the shared chart styles).
7. Import from CSV, the inverse of export.
8. Apple Watch, see `plans/watch.md`.
